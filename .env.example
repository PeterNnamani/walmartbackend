# Copy this file to .env and fill the values locally (DO NOT commit your real .env)
# For Render, set these in the service Environment settings instead of storing a file.

# SendGrid API key (preferred; do NOT paste this publicly)
SENDGRID_API_KEY=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

# Optional: From and To addresses used when sending mail
MAIL_FROM=peternnamani001@gmail.com
MAIL_TO=peternnamani001@gmail.com

# Optional: Gmail SMTP fallback (only needed if you want SMTP fallback)
GMAIL_USER=peternnamani001@gmail.com
GMAIL_PASS=uqtwxtmxuytvpfpy

# Optional: override SMTP host/port/secure if needed
#SMTP_HOST=smtp.gmail.com
#SMTP_PORT=465
#SMTP_SECURE=true
# Example environment variables for the backend
# Copy this to `.env` and fill in real values (don't commit `.env`)
GMAIL_USER=peternnamani001@gmail.com
GMAIL_PASS=uqtwxtmxuytvpfpy
# Optional overrides for SMTP
# SMTP_HOST defaults to smtp.gmail.com
SMTP_HOST=smtp.gmail.com
# SMTP_PORT can be 465 (SSL) or 587 (STARTTLS)
# SMTP_SECURE should be true for 465, false for 587
#SMTP_PORT=465
#SMTP_SECURE=true
