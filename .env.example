# Example environment variables for the backend
# Copy this to `.env` and fill in real values (don't commit `.env`)
GMAIL_USER=peternnamani001@gmail.com
GMAIL_PASS=uqtwxtmxuytvpfpy
# Optional overrides for SMTP
# SMTP_HOST defaults to smtp.gmail.com
SMTP_HOST=smtp.gmail.com
# SMTP_PORT can be 465 (SSL) or 587 (STARTTLS)
# SMTP_SECURE should be true for 465, false for 587
#SMTP_PORT=465
#SMTP_SECURE=true
